spring:
  application:
    name: auth-service

server:
  port: ${SERVER_PORT:8081}

jwt:
  secret: ${JWT_SECRET:msa-commerce-jwt-secret-key-must-be-at-least-256-bits-long}
  expiration-ms: 7200000  # 2시간

management:
  endpoints:
    web:
      exposure:
        include: health,info
